# Family-B (pre-boot/boot) leaked public surface — ratchets to zero.
# One entry per public (non-_) top-level def/class NOT in its module's __all__.
# See tap/guards/public_surface.py (req-service-boundary-family-b-surface).
#
# EMPTY: the pre-boot / boot public surface is fully sealed. Every non-`_` top-level
# def/class in tap/preboot.py, tap_boot/orchestrator.py, tap_boot/profile.py is in its
# module's __all__. A new public helper on these un-gateable layers must appear here
# (last resort) or, far more likely, be `_`-sealed — the ratchet permits only shrink.
