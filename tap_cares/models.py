"""tap_cares models.

The first concrete models (Collector, CollectionJob) land in Phase 3 / Phase 4
via the add-model skill. This module exists so Django registers tap_cares as a
model-owning app from the outset.
"""
