"""tap_auth-owned development-time guards.

Discovered by `tap.guards.discovery` via the filesystem (no import list to edit): drop a
module in here and its `Guard` subclass registers itself.
"""

from __future__ import annotations
