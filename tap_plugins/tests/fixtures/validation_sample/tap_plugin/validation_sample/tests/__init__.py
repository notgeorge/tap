"""validation_sample plugin tests.

The validate_plugin structure level checks that a tests/ dir exists inside the
package (it ships in the wheel and travels with the plugin). This fixture carries
no behavioural tests of its own — it IS the test subject for the tap_plugins
validate_plugin suite.
"""
