"""tap_plugins-owned development guards.

Discovered by the harness (`tap.guards.discovery`) via the filesystem walk. These
protect plugin-system invariants, so they live with tap_plugins rather than
centrally in `tap/guards/`.
"""
