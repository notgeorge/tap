{
  "slug": "ASSUMES_ROLE",
  "name": "Assumes Role",
  "description": "An IAM user, IAM role, Lambda function, or EventBridge rule assumes an IAM role.",
  "sources": [
    "aws_iam_user",
    "aws_iam_role",
    "aws_lambda",
    "aws_eventbridge_rule"
  ],
  "targets": [
    "aws_iam_role"
  ],
  "default_dimensions": {
    "tap.cloud": "aws"
  }
}
